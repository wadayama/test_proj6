# Medium-scale experiment 
# n=20 variables, 5000 trials

experiment:
  # Number of uniform random variables to sum in each trial
  n: 20
  
  # Number of trials to perform
  m: 5000
  
  # Random seed for reproducibility
  seed: 123

output:
  # Directory for saving results
  directory: "outputs"